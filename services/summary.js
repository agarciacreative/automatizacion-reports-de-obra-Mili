// Fase 3: generación de resumen ejecutivo con Claude
const Anthropic = require('@anthropic-ai/sdk');

const client = new Anthropic();

async function generarResumen(trabajos) {
  // TODO: implementar en Fase 3
  throw new Error('Resumen no implementado aún — pendiente Fase 3');
}

module.exports = { generarResumen };
